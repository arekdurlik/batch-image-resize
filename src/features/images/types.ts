export enum SortOption {
    FILENAME = 'FILENAME',
    FILESIZE = 'FILESIZE',
    VARIANT = 'VARIANT',
    INPUT_ORDER = 'INPUT_ORDER',
}

export enum SortDirection {
    ASC = 'ASC',
    DESC = 'DESC',
}
